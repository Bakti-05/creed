apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "creed.fullname" . }}-test-connection"
  labels:
    {{- include "creed.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "creed.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
